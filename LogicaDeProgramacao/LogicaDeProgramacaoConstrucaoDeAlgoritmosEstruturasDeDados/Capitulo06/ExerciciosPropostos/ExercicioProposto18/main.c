#include <stdio.h>
#include <string.h>

int main() {
    long long numero;
    printf("Digite um número (até 12 dígitos): ");
    scanf("%lld", &numero);
    //imprimirPorExtenso(numero);
    return 0;
}
